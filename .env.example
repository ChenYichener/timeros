# TimerOS 环境变量配置模板
# 复制此文件为 .env 并填写实际配置值
# cp .env.example .env

# ============================================
# MySQL数据库配置（必填）
# ============================================
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_mysql_password
MYSQL_DATABASE=timeros

# 数据库连接池配置（可选，使用默认值）
# MYSQL_POOL_SIZE=10
# MYSQL_MAX_OVERFLOW=20
# MYSQL_POOL_RECYCLE=3600

# ============================================
# AI服务配置（至少配置一个，用于AI功能）
# ============================================
# OpenAI配置
OPENAI_API_KEY=your_openai_api_key

# Anthropic配置（可选）
# ANTHROPIC_API_KEY=your_anthropic_api_key

# ============================================
# 应用配置
# ============================================
DEBUG=True
LOG_LEVEL=INFO

# ============================================
# 邮件客户端配置（可选，用于发送通知）
# ============================================
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_email@example.com
# SMTP_PASSWORD=your_email_password

# ============================================
# Notion客户端配置（可选，用于Notion集成）
# ============================================
# NOTION_API_KEY=your_notion_api_key

# ============================================
# Web搜索配置（可选，用于网络搜索功能）
# ============================================
# SERPAPI_KEY=your_serpapi_key

# AI提供商选择（可选）
# 可选值：openai, anthropic, local
# 如果不指定，将根据可用的API密钥自动选择（优先级：OpenAI > Anthropic > Local）
# AI_PROVIDER=openai
